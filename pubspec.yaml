name: mobile_app
description: SmartJ.

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  medcorder_audio: ^0.0.5
  flutter_statusbarcolor: ^0.2.3
  fancy_bottom_navigation: ^0.3.2
  permission_handler: ^4.2.0+hotfix.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
